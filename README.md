if something is not in a folder it can be downloaded and should work without needing any other scripts
if something is in a folder it needs whatever else is in the folder
SettingsUI also needs rich_counter.gd to work
